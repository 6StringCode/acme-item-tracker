X- ability to delete a thing
X- ability to create a user
X- ability to delete a user
X- a thing should have ranking which defaults to 1 (display it)
X- add the ability to increase or decrease the ranking of a thing
X- the home page should show the thing(s) with the highest ranking
X- a thing can belong to a user and a user can have many things. In seed logic have some of the things belong to users.
x- on the things view display the user who owns it (if they have an owner)
  (you don't need an include)
X- on the users view display the things a user owns
- add a select object on the things page for each thing, which allows you to assign the thing to a new user, or no user
